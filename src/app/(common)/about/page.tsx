import React from "react";

// export const dynamic = "force-dynamic"; // this will make the page dynamic and it will not be cached by the server

const AboutPage = async () => {
  // Simulate a delay to show the loading state
  // await new Promise((resolve) => setTimeout(resolve, 4000));

  // Simulate an error to show the error state
  // throw new Error("Something went wrong in the about page");
  return <div>About Page</div>;
};

export default AboutPage;
